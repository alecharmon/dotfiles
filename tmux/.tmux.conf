

# True color support
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

# Mouse support
set -g mouse on

# Start windows and panes at 1
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

# Increase scrollback
set -g history-limit 50000

# Reduce escape delay
set -sg escape-time 0

# Vi mode for copy
setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-selection-and-cancel
bind -T copy-mode-vi C-v send -X rectangle-toggle
set -g wrap-search on

# switch panes using Alt-arrow without prefix
# Wrap to previous/next window when at edge
bind -n C-Left run 'if [ "#{pane_at_left}" = "1" ]; then /opt/homebrew/bin/tmux previous-window; else /opt/homebrew/bin/tmux select-pane -L; fi'
bind -n C-Right run 'if [ "#{pane_at_right}" = "1" ]; then /opt/homebrew/bin/tmux next-window; else /opt/homebrew/bin/tmux select-pane -R; fi'
bind -n C-Up select-pane -U
bind -n C-Down select-pane -D

# Intuitive splits (open in current path)
bind | split-window -h -c "#{pane_current_path}" 'screensaver; exec $SHELL'
bind - split-window -v -c "#{pane_current_path}" 'screensaver; exec $SHELL'
unbind '"'
unbind %

# New windows default to ~/dev if it exists, otherwise ~
# Run screensaver on open, shell takes over when dismissed
if-shell "test -d ~/dev" \
  "bind c new-window -c ~/dev 'screensaver; exec $SHELL'" \
  "bind c new-window -c ~ 'screensaver; exec $SHELL'"

# Vim-style pane navigation
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Pane resizing
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# Reload config
bind r source-file ~/.tmux.conf \; display "Config reloaded"

# --- Gruvbox theme ---
set -g status-style "bg=#3c3836,fg=#ebdbb2"
set -g status-left '#{?client_prefix,#[bg=colour214 fg=colour235 bold] PREFIX ,#{?pane_in_mode,#{?selection_active,#[bg=#d3869b fg=#282828 bold] VISUAL ,#[bg=#b8bb26 fg=#282828 bold] NORMAL },#[bg=#458588 fg=#282828 bold] #S }}#[bg=#3c3836] '
set -g status-right "#[fg=#83a598]#H #[fg=#a89984]%Y-%m-%d #[fg=#ebdbb2]%H:%M "
set -g status-left-length 30
set -g status-right-length 60

setw -g automatic-rename on
setw -g automatic-rename-format "#{b:pane_current_path}:#{pane_current_command}"

setw -g window-status-format "#[fg=#a89984] #I:#W "
setw -g window-status-current-format "#[bg=#504945,fg=#fabd2f,bold] #I:#W "

set -g pane-border-style "fg=#504945"
set -g pane-active-border-style "fg=#458588"
set -g message-style "bg=#504945,fg=#ebdbb2"
